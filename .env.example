# Container runtime env for docker-compose
# Optional: copy to .env and adjust values for your environment.

# Image selection
# - Default: official pre-built image
# - Local build script (option 2) will override this to :local automatically
PLUGIN_IMAGE=obeginners/antigravity-403-plugin:latest

# Host path mappings
PLUGIN_CONFIG_PATH=./config.yaml
PLUGIN_AUTH_PATH=./auths
PLUGIN_LOG_PATH=./logs

# If you want to reuse CLIProxyAPI auth directly, point to its auths directory.
# Example:
# PLUGIN_AUTH_PATH=../CLIProxyAPI/auths

PLUGIN_CONFIG=/app/config.yaml
PLUGIN_LISTEN=0.0.0.0:9813

# Upstream CLIProxyAPI address reachable from container
# On Docker Desktop, host.docker.internal usually works.
CLIPROXY_UPSTREAM=http://host.docker.internal:8317

# Antigravity fallback chain
ANTIGRAVITY_UPSTREAM=https://daily-cloudcode-pa.googleapis.com,https://daily-cloudcode-pa.sandbox.googleapis.com

# Plugin behavior
PLUGIN_AUTH_DIR=/app/auths
PLUGIN_INJECT_AUTH_BASE_URL=true
PLUGIN_FORCE_AUTH_REFRESH=false
PLUGIN_SELF_CHECK=true
PLUGIN_SELF_CHECK_API_KEY=
PLUGIN_LOG_CLEANUP_INTERVAL=
